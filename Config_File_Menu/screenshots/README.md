# Screenshots

Add screenshots for Exchange upload here.

Recommended captures:

- `config-file-menu-expanded.png` - menu open with nested child icons visible.
- `config-file-menu-collapsed.png` - responsive/collapsed menu state.
- `config-file-menu-config.png` - Designer view showing `params.menuConfigType` and `params.menuConfig` on `Config File Menu/MenuContent`.
